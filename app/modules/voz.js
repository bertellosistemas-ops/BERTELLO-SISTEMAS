// Módulo de voz (stub, integración con WebSpeech API/Electron)
function escucharVoz(callback) {
  // Aquí iría la integración real con STT
  // callback(textoDetectado);
}
module.exports = { escucharVoz };
