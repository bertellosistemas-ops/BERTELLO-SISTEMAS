// Módulo de cámara (stub, integración con ZXing/Quagga en desarrollo)
function escanearCodigo(callback) {
  // Aquí iría la integración real con ZXing/Quagga
  // callback(codigoDetectado);
}
module.exports = { escanearCodigo };
